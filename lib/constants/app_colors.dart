import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Colors.white;

  // // Accent colors
  static const Color accentColor = Color(0xFFF42619);

  // Background colors
  static const Color backgroundColor = Colors.white;

  // Field color
  static const Color fieldColor = backgroundColor;

  //border color
  static const Color borderColor = Color(0xFFECECEC);

  // Text colors
  static const Color textColor = Color(0xFF322E2E);
  static const Color textColorLight = Colors.white;
  static const Color textColorSecondary = Color(0xFF656565);
  static const Color hintTextColor = Color(0xFF8F8F8F);
  static const Color buttonTextColor = backgroundColor;

  // Button colors
  static const Color buttonColor = AppColors.accentColor;

  //delete colors
  static const Color deleteColor = Color(0xffE3756D);

  //indicator
  // static const Color pending = Color(0xffFFC107);
  static const Color success = Color(0xff52BD94);
  static const Color failed = Color(0xffE3756D);
}
